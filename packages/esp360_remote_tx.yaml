remote_transmitter:
  - id: RF_TX
    pin: ${rf_tx_pin}
    carrier_duty_percent: 100%
    non_blocking: true
  - id: IR_TX
    pin:  ${ir_tx_pin}
    carrier_duty_percent: 50%
    non_blocking: true
